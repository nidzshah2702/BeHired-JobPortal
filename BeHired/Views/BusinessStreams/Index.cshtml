@model IEnumerable<BeHired.Models.BusinessStream>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.business_stream_name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.business_stream_name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.BusinessStreamId }) |
            @Html.ActionLink("Details", "Details", new { id=item.BusinessStreamId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.BusinessStreamId })
        </td>
    </tr>
}

</table>
