@model IEnumerable<BeHired.Models.JobApplication>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.JobPost.job_title)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Profile.Id)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.JobPost.job_title)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Profile.Id)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.JobApplicationId }) |
            @Html.ActionLink("Details", "Details", new { id=item.JobApplicationId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.JobApplicationId })
        </td>
    </tr>
}

</table>
